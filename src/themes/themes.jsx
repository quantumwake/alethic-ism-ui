import {amber, pro, matrix, hybrid, light, crimson} from "../themes/index";

const themes = {
    matrix: matrix,
    amber: amber,
    pro: pro,
    light: light,
    hybrid: hybrid,
    crimson: crimson,
};

export default themes;